import React from "react";

function VideoDetails({ selectedVideoDetails }) {
  return (
    <></>
    // <section className="details"> <h1>{selectedVideoDetails.title}</h1> </section>
  );
}

export default VideoDetails;
